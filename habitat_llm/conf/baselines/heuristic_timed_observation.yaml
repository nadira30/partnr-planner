# @package _global_

defaults:
  - /examples/centralized_full_obs
  - override /planner@evaluation.planner: timed_observation_planner
  - override /agent/@agents.agent_0.config: oracle_rearrange_agent_slow

# Override the base velocity action - make robot same speed as human
habitat:
  task:
    actions:
      agent_0_base_velocity:
        lin_speed: 10.0
        ang_speed: 10.0
